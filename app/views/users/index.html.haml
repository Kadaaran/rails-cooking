%h2 Listing users
%table
  %tr
    %th Name
    %th Email
    %th Company
  - @users.each do |user|
    %tr
      %td= user.name
      %td= user.email
      %td= user.company.name
      %td= link_to 'Delete', user_path(user), method: :delete
